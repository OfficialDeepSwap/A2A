{
  "name": "a2a-network",
  "version": "1.0.0",
  "description": "Agent-to-Agent encrypted communication network on EVM",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base_sepolia",
    "deploy:arbitrum": "hardhat run scripts/deploy.js --network arbitrum_sepolia",
    "deploy:polygon": "hardhat run scripts/deploy.js --network polygon_amoy",
    "interact": "hardhat run scripts/interact.js",
    "clean": "hardhat clean"
  },
  "keywords": [
    "agent",
    "a2a",
    "encryption",
    "ethereum",
    "smart-contracts"
  ],
  "author": "DeepSwap",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
